# Disable PHP execution in the book cover uploads directory.

# Prevents an attacker from uploading a disguised PHP file and executing it.

<FilesMatch "\.php[0-9]?$">
Require all denied
</FilesMatch>
php_flag engine off
Options -ExecCGI -Indexes
